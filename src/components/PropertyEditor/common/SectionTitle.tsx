import { Typography, styled } from '@mui/material';

/**
 * Stilisierte Komponente für Abschnittstitel im PropertyEditor.
 */
export const SectionTitle = styled(Typography)`
  font-weight: 500;
  margin-top: 1rem;
`;
